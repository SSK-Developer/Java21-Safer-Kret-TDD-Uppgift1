package org.example;

import org.example.model.Book;
import org.example.model.Books;
import org.example.service.BookingService;
import org.example.service.PaymentService;

import java.util.ArrayList;

public class Main {

    private static PaymentService paymentService;

    public static void main(String[] args) {
/*
        BookingRequest bookingRequestLoan1 = new BookingRequest("1", "Dream Town", 100, true);
        BookingRequest bookingRequestReturn1 = new BookingRequest("Dream Town", true);

        BookingRequest bookingRequestLoan2 = new BookingRequest("2", "Book Lovers", 100, true);
        BookingRequest bookingRequestReturn2 = new BookingRequest("Book Lovers", true);

        BookingRequest bookingRequestLoan3 = new BookingRequest("3", "Abandoned in Death", 100, true);
        BookingRequest bookingRequestReturn3 = new BookingRequest("Abandoned in Death", true);

        BookingRequest bookingRequestLoan4 = new BookingRequest("4", "The Paris Apartment", 100, true);
        BookingRequest bookingRequestReturn4 = new BookingRequest("The Paris Apartment", true);

        BookingService bookingService = new BookingService(paymentService);

        System.out.println("-------------------------------------");

        bookingService.loanBook(bookingRequestLoan1);
        bookingService.loanBook(bookingRequestLoan2);
        bookingService.loanBook(bookingRequestLoan3);
        bookingService.loanBook(bookingRequestLoan4);

        System.out.println("-------------------------------------");

        bookingService.returnBook(bookingRequestReturn1);
        bookingService.returnBook(bookingRequestReturn2);
        bookingService.returnBook(bookingRequestReturn3);
        bookingService.returnBook(bookingRequestReturn4);

        System.out.println("-------------------------------------");

        bookingService.loanBook(bookingRequestLoan1);
        bookingService.loanBook(bookingRequestLoan2);
        bookingService.loanBook(bookingRequestLoan3);
        bookingService.loanBook(bookingRequestLoan4);

        System.out.println("-------------------------------------");

        bookingService.returnBook(bookingRequestReturn1);
        bookingService.returnBook(bookingRequestReturn2);
        bookingService.returnBook(bookingRequestReturn3);
        bookingService.returnBook(bookingRequestReturn4);

        System.out.println("-------------------------------------");

        bookingService.searchByRate(2);

        System.out.println("-------------------------------------");

 */

    }
}